node_modules
# The app's own build output only. NOT vendor/*/dist -- the vendored SDK is
# committed on purpose, so a clone without the pool repo still builds.
/dist
src/deployment.json
.env
src/pools.bundle.mjs
src/eligibility.bundle.mjs
